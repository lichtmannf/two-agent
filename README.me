#Next step of AI Journey with multi agent setup

Multi agent project, using langgraph, with two initial use cases:

1. Reading contenst of a CSV file and generating statistics
2. Working with classes, in this case a transaction classes

Both use researcher and writer. Each agent can decide which tools to use. Use are also available in the project. The project is model agnostic. In the meanwhile, is using ollama, local and with a remot PC (GPU).

A.